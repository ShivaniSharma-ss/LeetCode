M-1 Using Hashing


T.c O(nlogn)+O(n)
S.C O(n)

M-2 class Solution {
    public int removeDuplicates(int[] nums) {
       int j=0;
        int n=nums.length;
        for(int i=0;i<n-1;i++)
        {
            if(nums[i]!=nums[i+1])
            {
                nums[j]=nums[i];
                j++;
            }
            
        }
        nums[j]=n-1;
        return j+1;
        }}
        
        T.C O(N)
        S.C O(1)
